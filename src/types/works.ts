export type Work = {
  id: string;
  title: string;
  body: string;
  siteLink: string;
  gitLink: string;
  date: string;
  caption: string;
  thumbnail: {
    url: string;
  };
  image: {
    url: string;
  };
}
